namespace SF3.Types {
    public enum ModelCollectionType {
        PrimaryModels = 0,
        Chunk19Model  = 1, // Seen in Scn1 Z_AS.MPD
        Chunk1Model   = 2, // The Kraken, failed ice spell against Galm
    }
}
